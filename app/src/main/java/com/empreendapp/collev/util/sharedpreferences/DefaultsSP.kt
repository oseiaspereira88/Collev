package com.empreendapp.collev.util.sharedpreferences

class DefaultsSP {
    companion object{
        val PACKAGE = "com.empreendapp.collev"
        val PREF = PACKAGE + ".PREF"
        val ID = PACKAGE + ".models.User.ID"
        val NOME = PACKAGE + ".models.User.NOME"
        val EMAIL = PACKAGE + ".models.User.EMAIL"
        val TIPO = PACKAGE + ".models.User.TIPO"
        val CONFIG = PACKAGE + ".CONFIG"
        val USER_CACHE = PACKAGE + ".USER_CACHE"
    }
}